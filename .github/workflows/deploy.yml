name: Upload Data Folder to S3

on:
  push:
    branches:
      - main
      - development
    # Cambiado aquí para que coincida con tu carpeta real
    paths:
      - '1. Data/**'

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Configure AWS Credentials
      uses: aws-actions/configure-aws-credentials@v4
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: us-east-1

    # Cambiado aquí también en el comando de origen de S3 sync
    - name: Sync Data Folder to S3
      run: |
<<<<<<< HEAD
        aws s3 sync "./1. Data" s3://your-bucket-name/Data --delete
=======
        aws s3 sync "./1. Data" s3://dataengineeringaws01/Data --delete
>>>>>>> 593587aa13baa1081b939ff495d82203d619b18a
